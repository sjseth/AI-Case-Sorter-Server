# Claude Code project instructions (local only, not pushed)
CLAUDE.md

# Python bytecode cache
__pycache__/
*.pyc

# setup.py marker files (local environment state)
.torch_setup_complete
.server_deps_complete

# Local virtual environment created by startserver.sh
.venv/

# Server data root (registry DB, training images, checkpoints, logs, token cache)
data/

# pytest / tooling caches
.pytest_cache/
